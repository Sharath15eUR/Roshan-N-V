#include <stdio.h>

int main()
{
    int n;
    printf("Enter the number:");
    scanf("%d",&n);
    int set=0;
    while(n>=1)
    {
        set+=n&1;
        n>>=1;
    }
    printf("Set bits = %d", set);
}
